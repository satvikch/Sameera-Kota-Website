// Deprecated — the old "trust strip" band has been folded into the Hero
// as the metadata grid (Surgeon / Discipline / Location / Experience).
// Kept as a thin placeholder export so old references don't break.
import React from 'react';

export const TrustStrip: React.FC = () => null;
